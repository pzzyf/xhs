# P7：公网部署 + 线上验收 + 文档收尾

> 目标：Alchemy 部署 Worker+D1+R2 到公网；Native `EXPO_PUBLIC_SERVER_URL` 指向线上；按 requirements §4 剧本全量验收；README 演示说明。
> 结束标志：DoD——公网 Workers + iOS/Android 模拟器连线上 API 走通 §2 剧本；开箱有种子内容。

## 1. 部署（BRIEF §7 约束）

- [x] Cloudflare 凭据：使用 `~/.alchemy` OAuth profile（不入库；`packages/infra/.env` 非必需）
- [x] `apps/server/.env` 配线上 `BETTER_AUTH_URL=https://xhs-server.0624afe1.workers.dev`、`SEED_SECRET`；CORS 保留本地 web 源；`trustedOrigins` 增加 `exp://`（Expo Go 开发 origin，修复原生 403）
- [x] `bun run alchemy:plan` → 确认 → `bun run alchemy:deploy -- --yes`（首次部署 + 改 URL 后重部署）
- [x] 线上 `GET /` 健康检查：`https://xhs-server.0624afe1.workers.dev/` → 200 `{"ok":true,...}`（经本机 HTTP 代理；直连被本网阻断但平台侧 API 已核实部署）
- [x] 线上种子：D1 已应用迁移，并通过 Cloudflare D1 query API 写入 demo 用户 + 16 条笔记（user=1 / notes=16，AC-19 数据侧通过；R2 图片对象因 OAuth 无 r2 scope 未上传）
- [x] 线上 R2 图片：经公网 seed 接口补传 16 张占位图（`imagesUploaded:16`），`GET /images/seed/note-01.png` → 200 image/png

## 2. Native 连线上

- [x] `apps/native/.env`：`EXPO_PUBLIC_SERVER_URL=https://xhs-server.0624afe1.workers.dev`（不入库；`.env.example` 注释已更新）
- [x] Android 模拟器已指向线上 API（Expo Go + Maestro 完整录证：首页/详情来自生产；原生注册 `MaestroPublish`；原生发布（选图/裁剪/上传 200/创建 200，笔记 id=22 在线上 D1，R2 图 200，首页与我的可见）；原生点赞 3→4→3（toggle 200×2）；设置退出 sign-out 200 + 匿名门禁）
- [~] iOS 模拟器：已装 Expo Go 并尝试连接，无头环境下未能连上 Metro/线上 API，未录证

## 3. 全量验收（requirements §4 剧本 AC-01…AC-10）

- [x] AC-01…AC-09：Web 端对线上 API 全量走通（经同源面纱转发到公网 Worker：冷启动双列流/详情/未登录点赞跳登录/注册/点赞+1-1/发布单图/我的/设置退出/写操作再要求登录；控制台 0 error）
- [x] AC-10：`GET /` 公网可达（经代理 200）；`EXPO_PUBLIC_SERVER_URL` 指向该部署
- 验收方式：Web 全量录证 + 公网 HTTP 全链路（signup/upload/create/like/me/401）+ Android 模拟器启动并打到同一线上 API；iOS 模拟器未跑

## 4. 文档收尾

- [x] 重写根 `README.md`：演示说明（本地启动、种子、部署、线上 URL 配置、验收剧本与网络注意事项）
- [x] 更新 `AGENTS.md` 当前阶段：与 V1 真源码/实际状态一致
- [x] 更新 `workflow-state.md`：roadmap 逐项核对

## 5. 验证与收尾

- [x] 最终 `bun run check-types`；scoped Biome；`bunx expo install --check`
- [x] 汇报：部署 URL、AC 矩阵逐项证据、未覆盖项及原因
- [x] 状态：**deployed（公网 HTTP + Web + Android 模拟器原生全流程（含发布）录证通过；iOS 模拟器待有界面环境补录）**

## 6. 阶段收尾

- 若用户要求 commit：提交策略 user-managed，提交时附带证据；不 force push、不绕过 hooks。
