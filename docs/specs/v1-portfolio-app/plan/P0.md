# P0：仓库清空 + Monorepo 骨架

> 目标：执行 BRIEF §7「全删重来」，重建 Bun workspaces + Turbo + Biome 骨架，根目录 `bun run check-types` 通过。
> 结束标志：`bun run check-types` 全绿（骨架阶段允许工作区为最小可编译状态）；无旧业务代码残留。

## 0. 删除清单（BRIEF §9-3，用户确认后执行）

删除以下路径（保留 `.git`、`.agents/skills/native-data-fetch`、`.vscode`）：

| 路径 | 理由 |
|------|------|
| `apps/native/*`（旧业务：features/home、auth、input、theme、providers、lib、components、app 路由） | 按 V1 重建（NG-14：不得 copy-paste 旧实现） |
| `apps/server/*`（旧 src/dev.ts、index.ts、worker.ts、rpc、tsdown 双配置） | NG-12 移除 Lambda/Node 双运行时 |
| `apps/web/` | NG-11 移除 |
| `packages/api/*`、`packages/auth/*`、`packages/db/*`（含 Kysely）、`packages/env/*`、`packages/infra/wrangler.jsonc` | NG-13 双轨 IaC 移除；ORM 换 Drizzle（DEC-06） |
| `docs/specs/api-contract/`、`docs/specs/xhs-product-roadmap/` | 旧规格，与 V1 冲突（BRIEF §7.1） |
| 根 `AGENTS.md`、`README.md`、`readme.md` | 重建为与 V1 真源码一致（BRIEF §7.1） |

保留并复用：`package.json`（workspaces 结构）、`turbo.json`、`biome.json`、`lefthook.yml`、`tsconfig.json`、`bts.jsonc`、`.gitignore`、`packages/config/tsconfig.base.json`。

## 1. 清空执行

- [ ] `git rm -r` 上述路径（保留 `.git`），提交与否由用户决定（user-managed）。
- [ ] 清理残留 node_modules/dist（可选 `bun install` 重新生成 lock）。

## 2. 重建骨架

文件清单（全部新建或重写）：

| 文件 | 内容 |
|------|------|
| `package.json`（根） | 保留 workspaces `apps/*` + `packages/*`；scripts：`dev`、`dev:native`、`dev:server`、`check-types`、`check`、`alchemy*`（同现结构） |
| `packages/config/tsconfig.base.json` | 沿用（strict、ESNext、moduleResolution bundler） |
| `packages/env/` | `src/index.ts`（server/native 分支）从 `@t3-oss/env-core` 或 Zod 起骨架；`package.json` 挂 `@xhs/config` |
| `packages/api/` | 空骨架：`src/index.ts` 导出 `contract` 占位（oRPC `contract()` 空对象），P3 填过程 |
| `packages/db/` | 空骨架：`src/schema.ts` 占位 + `drizzle.config.ts`；P1 填 schema/迁移 |
| `packages/auth/` | 空骨架：`src/index.ts` 占位；P2 填 better-auth |
| `packages/infra/` | 重写 `alchemy.run.ts`（声明 D1 `xhs-d1`、R2 `xhs-images`、Worker `xhs-server`、migrationsDir 指向 `packages/db/migrations`）；`.env.example` |
| `apps/server/` | `src/worker.ts`（导出 Hono fetch 壳）+ `src/app.ts`（logger、CORS、`GET /` 健康检查）；P1 起 RPC/binding |
| `apps/native/` | 重建 Expo Router 最小壳：`app/_layout.tsx`（Query+SafeArea+HeroUI+Theme+Auth 占位）+ `app/(tabs)/index.tsx` 占位 + `package.json`/`app.json`/`tsconfig.json`/`metro.config.js`/`babel` 配置；依赖用 `bunx expo install` 对齐 SDK |
| `AGENTS.md`、`README.md` | 重写为与 V1 结构一致 |

依赖安装（根目录）：

- [ ] `bun install`
- [ ] `apps/native`：`bunx expo install --check` 校验对齐

## 3. 验证

- [ ] 根目录 `bun run check-types` 通过
- [ ] 对改动文件 `bunx biome check`（scoped，避免全仓写入污染）
- [ ] `bunx turbo run check-types --dry` 可发现全部 workspace 任务（可选）

## 4. 阶段收尾

- 汇报：删除清单执行结果、骨架结构、`check-types`/Biome 输出。
- 状态：awaiting-human-review（P0 结束）。
