# P5：点赞 toggle + 登录拦截 + 回首页策略

> 目标：详情页点赞/取消（计数 +1/-1、已赞态切换）；未登录点赞跳登录；登录成功回首页。
> 结束标志：AC-03/AC-05 通过（未登录点赞→登录页；登录后同一详情点赞取消正确）。

## 1. 契约扩展（packages/api）

- [ ] `likes.toggle({ noteId: string })` → `{ liked: boolean, likeCount: number }`（鉴权）
- [ ] 错误码：未登录 401、noteId 不存在 404

## 2. Server 实现（apps/server/src/rpc/）

- [ ] `likes.toggle`：事务内——存在该用户对 noteId 的 like 行则删除（unlike），否则插入（like）；返回最新 `liked` 与 `likeCount = count(*)`
- [ ] 验证：`alchemy dev` 下带会话 toggle 两次计数回原值；不存在的 note 404；未登录 401；并发重复调用幂等（一人一赞）

## 3. Native 详情页交互（apps/native/app/note/[id].tsx）

- [ ] 赞按钮 + 计数：未登录显示计数；点击 → 判断未登录 → `router.push("/sign-in")`（登录成功回首页，不回详情——NG-17 不回原操作）
- [ ] 已登录：乐观更新（pending 态禁重复点击）+ 失败回滚 + Toast
- [ ] 使用 `useMutation` + 详情 query 更新缓存（`likeCount`/`viewerHasLiked` 就地更新，不整页刷新）
- [ ] 重新进入详情赞态/计数与服务端一致（AC-05 再进同一详情验证）

## 4. 验证

- [ ] Web/模拟器：未登录点赞跳登录（AC-03）；注册后回首页再进详情点赞 +1/已赞，取消 -1（AC-05）
- [ ] 一人一赞幂等：重复点击无重复计数
- [ ] `bun run check-types`；scoped Biome

## 5. 阶段收尾

- 汇报：toggle 实现、交互截图/输出、幂等验证结果。
- 状态：awaiting-human-review。
