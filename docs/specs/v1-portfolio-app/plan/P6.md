# P6：我的主页 + 设置退出 + 中文 UI 打磨

> 目标：我的主页（默认头像+昵称+我的笔记列表）、设置页（昵称/头像占位+退出登录）、全站中文与视觉打磨。
> 结束标志：AC-08/AC-09 通过；界面统一现代干净，无英文残留 UI 文案。

## 1. 契约扩展（packages/api）

- [x] `me.notes()` → `NoteListItem[]`（鉴权，仅自己，时间倒序）
- [x] `me.profile()` → `{ id, name, email, image }`（鉴权；image V1 恒 null）

## 2. Server 实现（apps/server/src/rpc/）

- [x] `me.notes`：按当前用户 authorId 查询
- [x] `me.profile`：返回当前用户基础字段
- [x] 验证：带会话返回正确；未登录 401；换账号只见自己笔记

## 3. Native 我的主页（apps/native/app/(tabs)/me.tsx）

- [x] 未登录：默认头像占位 + CTA「去登录」（跳 `/sign-in`，成功回首页）+ 空态说明
- [x] 已登录：默认头像（本地 asset）+ 昵称 + 我的笔记列表（复用卡片组件/查询）；列表点击进详情
- [x] 「设置」入口（右上角或列表项）→ `/settings`

## 4. Native 设置页（apps/native/app/settings.tsx）

- [x] 展示当前昵称 + 默认头像占位
- [x] 「退出登录」按钮 → `auth.signOut()` + 清 SecureStore + 回首页
- [x] 退出后：浏览首页正常；写操作再次要求登录（AC-09）

## 5. UI 打磨（P3/P4/P6 累积）

- [x] 全局：中文文案核对（loading/空态/错误/按钮/Toast）；主色青绿 + 中性灰底统一 token
- [x] 组件复用：笔记卡片（`components/note-card.tsx`）、头像占位（`components/avatar.tsx`）、页头（`components/page-header.tsx`）
- [x] 双列瀑布流视觉：封面 3:4、标题 ≤2 行、作者昵称小字、间距统一
- [ ] iOS 状态栏/安全区处理正确（至少验证一个真实平台，不只 Web）—— 本环境无 iOS 模拟器，未实测；全屏均用 SafeAreaView

## 6. 验证

- [x] 我的/设置/退出全流程（AC-08/AC-09）
- [x] 登录前后首页一致性（AC-18）
- [x] `bunx expo install --check`；`bun run check-types`；scoped Biome

## 7. 阶段收尾

- 汇报：我的/设置截图、退出语义验证、UI 打磨项。
- 状态：awaiting-human-review。
